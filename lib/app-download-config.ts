/**
 * 로아로골 모바일 앱 다운로드 링크.
 *
 * 스토어 심사 승인 후 App Store / Play Store 실제 URL을 아래 두 값에 채워 넣고
 * 커밋·푸시하면 그 시점부터 배포되는 배지가 정식 링크를 가리킨다.
 * 배지 자체는 항상 클릭 가능한 정식 디자인으로 렌더링된다(별도 "심사 중" 상태 없음).
 */
export const APP_STORE_URL = '';
export const PLAY_STORE_URL = '';
